/**
 * Домен telematics: агрегаты, value objects, доменные события, доменные исключения.
 * DeviceBinding — привязка устройства к полису; DriverScore — вычисляемое значение.
 * Не зависит от Spring, Kafka и транспорта — проверяется ArchitectureTest.
 */
package dev.ubi.telematics.domain;
