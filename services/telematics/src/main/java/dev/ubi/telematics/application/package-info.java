/**
 * Сценарии использования (command/query handlers) и порты — интерфейсы, которые реализуют адаптеры.
 * Здесь проходят границы транзакций. Зависит только от domain.
 */
package dev.ubi.telematics.application;
